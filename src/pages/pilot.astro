---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

const title = 'Programme Pilote | R.A.I.S.E.';
const description =
  "Devenez partenaire Early Adopter. Co-construisez la v1.0 et bénéficiez d'un accompagnement sur-mesure.";

const GOOGLE_SCRIPT_URL =
  'https://script.google.com/macros/s/AKfycbzEJqLNKILLdCQo6GuQGrDhMPlkcZ2kWRFz58vkgysAA1-3YvLPT5GKOWE6Hp3Qib23/exec';
---

<html lang="fr" class="scroll-smooth">
  <head>
    <BaseHead title={title} description={description} />
  </head>
  <body class="bg-slate-50 text-slate-900 font-sans antialiased">
    <Header />

    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12 md:py-20">
      <div class="text-center mb-16 animate-fade-in">
        <div
          class="inline-flex items-center gap-2 px-3 py-1 rounded border border-rust/30 bg-orange-50 text-rust text-[10px] font-mono uppercase tracking-widest mb-6"
        >
          <span class="w-2 h-2 rounded-full bg-rust animate-pulse"></span> 3 Slots Disponibles
        </div>
        <h1 class="text-4xl md:text-6xl font-extrabold text-slate-900 mb-6">
          Rejoindre le <span class="text-rust">Programme Pilote</span>
        </h1>
        <p class="text-xl text-slate-600 max-w-2xl mx-auto">
          Nous recherchons des partenaires industriels exigeants pour éprouver le moteur R.A.I.S.E.
          sur des cas réels avant la sortie publique.
        </p>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-12 gap-12 max-w-6xl mx-auto">
        <div class="lg:col-span-7">
          <div
            class="bg-white p-8 md:p-10 rounded-3xl shadow-xl border border-slate-200 relative overflow-hidden"
          >
            <div
              id="success-message"
              class="hidden absolute inset-0 bg-white z-20 flex flex-col items-center justify-center text-center p-8 animate-fade-in"
            >
              <div
                class="w-20 h-20 bg-green-100 rounded-full flex items-center justify-center text-4xl mb-6"
              >
                🚀
              </div>
              <h3 class="text-2xl font-bold text-slate-900 mb-2">Candidature Reçue !</h3>
              <p class="text-slate-600 mb-8">
                Merci pour votre intérêt. Nos ingénieurs vont analyser votre cas d'usage. Si vous
                êtes éligible, vous recevrez une invitation sous 48h.
              </p>
              <a href="/" class="text-rust font-bold hover:underline">Retour à l'accueil</a>
            </div>

            <h2 class="text-2xl font-bold text-slate-900 mb-6">Dossier de Candidature</h2>

            <form id="pilot-form" class="space-y-6">
              <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div>
                  <label for="nom" class="block text-sm font-bold text-slate-700 mb-2"
                    >Nom complet</label
                  >
                  <input
                    type="text"
                    name="nom"
                    id="nom"
                    class="w-full px-4 py-3 rounded-lg bg-slate-50 border border-slate-200 focus:border-rust focus:ring-1 focus:ring-rust outline-none transition-all"
                    placeholder="Jean Dupont"
                    required
                  />
                </div>
                <div>
                  <label for="email" class="block text-sm font-bold text-slate-700 mb-2"
                    >Email professionnel</label
                  >
                  <input
                    type="email"
                    name="email"
                    id="email"
                    class="w-full px-4 py-3 rounded-lg bg-slate-50 border border-slate-200 focus:border-rust focus:ring-1 focus:ring-rust outline-none transition-all"
                    placeholder="jean@industrie.com"
                    required
                  />
                </div>
              </div>

              <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div>
                  <label for="company" class="block text-sm font-bold text-slate-700 mb-2"
                    >Entreprise</label
                  >
                  <input
                    type="text"
                    name="company"
                    id="company"
                    class="w-full px-4 py-3 rounded-lg bg-slate-50 border border-slate-200 focus:border-rust focus:ring-1 focus:ring-rust outline-none transition-all"
                    placeholder="Nom de votre société"
                    required
                  />
                </div>
                <div>
                  <label for="role" class="block text-sm font-bold text-slate-700 mb-2"
                    >Fonction</label
                  >
                  <select
                    name="role"
                    id="role"
                    class="w-full px-4 py-3 rounded-lg bg-slate-50 border border-slate-200 focus:border-rust focus:ring-1 focus:ring-rust outline-none transition-all"
                  >
                    <option value="CTO / DSI">CTO / DSI</option>
                    <option value="Directeur Innovation">Directeur Innovation</option>
                    <option value="Responsable Métier">Responsable Métier (Ops)</option>
                    <option value="Architecte Système">Architecte Système</option>
                    <option value="Autre">Autre</option>
                  </select>
                </div>
              </div>

              <div>
                <label for="usecase" class="block text-sm font-bold text-slate-700 mb-2"
                  >Description du Cas d'Usage</label
                >
                <textarea
                  name="usecase"
                  id="usecase"
                  rows="4"
                  class="w-full px-4 py-3 rounded-lg bg-slate-50 border border-slate-200 focus:border-rust focus:ring-1 focus:ring-rust outline-none transition-all"
                  placeholder="Ex: Analyse de 5000 PDF techniques..."
                  required></textarea>
              </div>

              <div class="pt-4 border-t border-slate-100 mt-4">
                <label for="question" class="block text-sm font-bold text-slate-900 mb-2">
                  Une question avant de postuler ? <span
                    class="text-slate-400 font-normal text-xs ml-1">(Optionnel)</span
                  >
                </label>
                <textarea
                  name="question"
                  id="question"
                  rows="2"
                  class="w-full px-4 py-3 rounded-lg bg-slate-50 border border-slate-200 focus:border-rust focus:ring-1 focus:ring-rust outline-none transition-all placeholder:text-slate-400"
                ></textarea>
              </div>

              <div class="flex items-start gap-3 pt-2">
                <input
                  type="checkbox"
                  name="nda_check"
                  id="nda"
                  class="mt-1 w-4 h-4 text-rust border-slate-300 rounded focus:ring-rust"
                />
                <label for="nda" class="text-sm text-slate-600">
                  Nous aurons besoin d'un NDA (Accord de Confidentialité).
                </label>
              </div>

              <button
                id="submit-btn"
                type="submit"
                class="w-full bg-slate-900 hover:bg-rust text-white font-bold py-4 rounded-xl transition-all shadow-lg hover:shadow-xl flex items-center justify-center gap-2 group"
              >
                <span>Soumettre ma candidature</span>
                <span class="group-hover:translate-x-1 transition-transform">→</span>
              </button>

              <p id="error-msg" class="hidden text-center text-red-500 text-sm mt-2">
                Erreur lors de l'envoi. Veuillez réessayer.
              </p>
            </form>
          </div>
        </div>

        <div class="lg:col-span-5 space-y-8">
          <div class="bg-slate-900 text-white p-8 rounded-3xl shadow-xl relative overflow-hidden">
            <div class="absolute top-0 right-0 w-64 h-64 bg-rust/20 rounded-full blur-[80px]"></div>
            <h3 class="text-xl font-bold mb-6 relative z-10">Pourquoi ce programme ?</h3>
            <ul class="space-y-6 relative z-10">
              <li class="flex gap-4">
                <div
                  class="w-10 h-10 bg-white/10 rounded-lg flex items-center justify-center shrink-0 border border-white/20"
                >
                  🚀
                </div>
                <div>
                  <h4 class="font-bold text-rust text-sm uppercase">Accès Anticipé</h4><p
                    class="text-sm text-slate-300 mt-1"
                  >
                    Accès aux builds Alpha/Beta.
                  </p>
                </div>
              </li>
              <li class="flex gap-4">
                <div
                  class="w-10 h-10 bg-white/10 rounded-lg flex items-center justify-center shrink-0 border border-white/20"
                >
                  💎
                </div>
                <div>
                  <h4 class="font-bold text-rust text-sm uppercase">Conditions "Founder"</h4><p
                    class="text-sm text-slate-300 mt-1"
                  >
                    Tarif préférentiel à vie.
                  </p>
                </div>
              </li>
            </ul>
          </div>

          <div class="bg-orange-50 p-8 rounded-3xl border border-rust/20">
            <h3 class="text-lg font-bold text-slate-900 mb-4">Critères d'éligibilité</h3>
            <ul class="space-y-3 text-sm text-slate-700">
              <li class="flex items-center gap-2">
                <span class="text-green-600">✓</span> Besoin On-Premise / Souverain.
              </li>
              <li class="flex items-center gap-2">
                <span class="text-green-600">✓</span> Migration Cloud Souverain (ex: S3NS).
              </li>
              <li class="flex items-center gap-2">
                <span class="text-green-600">✓</span> Cas d'usage critique.
              </li>
            </ul>
          </div>
        </div>
      </div>
    </main>
    <Footer />

    <script define:vars={{ GOOGLE_SCRIPT_URL }}>
      const form = document.getElementById('pilot-form');
      const submitBtn = document.getElementById('submit-btn');
      const successMsg = document.getElementById('success-message');
      const errorMsg = document.getElementById('error-msg');

      form.addEventListener('submit', async (e) => {
        e.preventDefault();

        submitBtn.disabled = true;
        submitBtn.querySelector('span').textContent = 'Envoi en cours...';
        errorMsg.classList.add('hidden');

        try {
          // 1. On capture les données du formulaire
          const formData = new FormData(form);

          // 2. Gestion manuelle de la checkbox NDA (car une checkbox non cochée n'envoie rien)
          // On ajoute manuellement la clé "nda" qui correspond à la colonne du Sheet
          formData.append('nda', form.querySelector('#nda').checked ? 'OUI' : 'NON');

          // 3. Envoi simple via Fetch (Pas de JSON, on envoie le FormData direct)
          // C'est ce format que le script "Contact" attend (e.parameter)
          await fetch(GOOGLE_SCRIPT_URL, {
            method: 'POST',
            body: formData,
            // Note: Pas besoin de headers Content-Type, fetch le fait auto pour FormData
          });

          // 4. Succès (On assume le succès car 'no-cors' rend la réponse opaque, mais ça marche)
          form.classList.add('opacity-0');
          setTimeout(() => {
            successMsg.classList.remove('hidden');
            successMsg.classList.add('flex');
          }, 300);
        } catch (err) {
          console.error('Erreur:', err);
          submitBtn.disabled = false;
          submitBtn.querySelector('span').textContent = 'Soumettre ma candidature';
          errorMsg.classList.remove('hidden');
        }
      });
    </script>
  </body>
</html>
