---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

const title = 'Auditer mon besoin | R.A.I.S.E.';
const description =
  "Décrivez votre contexte opérationnel. Nous évaluons la faisabilité d'une architecture Neuro-Symbolique.";

const GOOGLE_SCRIPT_URL =
  'https://script.google.com/macros/s/AKfycbylPF-nJq2nY44sg3nRocy4lGmeHqJWBaDLAi1lJZtEU1kYwYH5yUPCzeXAIPSQFfIP6g/exec';
---

<!doctype html>
<html lang="fr" class="scroll-smooth">
  <head>
    <BaseHead title={title} description={description} />
  </head>
  <body class="bg-slate-50 text-slate-900 font-sans antialiased">
    <Header />

    <main class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-12 md:py-20">
      <div class="text-center mb-12 animate-fade-in">
        <h1 class="text-3xl md:text-5xl font-extrabold text-slate-900 mb-6">
          Lancer l'<span class="text-rust">Audit</span>
        </h1>
        <p class="text-lg text-slate-600 max-w-2xl mx-auto">
          Pour garantir une réponse pertinente, merci de détailler la nature de votre flux de
          données et vos contraintes critiques.
        </p>
      </div>

      <div
        class="bg-white rounded-4xl] p-8 md:p-12 shadow-xl border border-slate-200 relative overflow-hidden"
      >
        <div
          id="success-message"
          class="hidden absolute inset-0 bg-white z-20 flex-col items-center justify-center text-center p-8"
        >
          <div
            class="w-20 h-20 bg-green-100 text-green-600 rounded-full flex items-center justify-center text-4xl mb-6"
          >
            ✓
          </div>
          <h3 class="text-2xl font-bold text-slate-900 mb-2">Demande Reçue</h3>
          <p class="text-slate-600 max-w-md mx-auto">
            Votre contexte a été transmis à nos architectes. Nous analyserons la faisabilité et
            reviendrons vers vous sous 48h.
          </p>
          <a
            href="/"
            class="mt-8 inline-block bg-slate-900 text-white font-bold py-3 px-8 rounded-full"
            >Retour à l'accueil</a
          >
        </div>

        <form id="audit-form" class="space-y-8 relative z-10">
          <div>
            <h3
              class="text-sm font-bold text-slate-400 uppercase tracking-widest mb-6 border-b border-slate-100 pb-2"
            >
              1. Qui êtes-vous ?
            </h3>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
              <div>
                <label for="nom" class="block text-sm font-bold text-slate-700 mb-2"
                  >Nom complet</label
                >
                <input
                  type="text"
                  name="nom"
                  id="nom"
                  required
                  class="w-full px-4 py-3 rounded-xl bg-slate-50 border border-slate-200 focus:border-rust focus:ring-1 focus:ring-rust outline-none transition-colors"
                  placeholder="Jean Dupont"
                />
              </div>
              <div>
                <label for="email" class="block text-sm font-bold text-slate-700 mb-2"
                  >Email professionnel</label
                >
                <input
                  type="email"
                  name="email"
                  id="email"
                  required
                  class="w-full px-4 py-3 rounded-xl bg-slate-50 border border-slate-200 focus:border-rust focus:ring-1 focus:ring-rust outline-none transition-colors"
                  placeholder="jean.dupont@entreprise.com"
                />
              </div>
              <div class="md:col-span-2">
                <label for="entreprise" class="block text-sm font-bold text-slate-700 mb-2"
                  >Entreprise & Secteur</label
                >
                <input
                  type="text"
                  name="entreprise"
                  id="entreprise"
                  class="w-full px-4 py-3 rounded-xl bg-slate-50 border border-slate-200 focus:border-rust focus:ring-1 focus:ring-rust outline-none transition-colors"
                  placeholder="Nom de l'organisation & Secteur d'activité"
                />
              </div>
            </div>
          </div>

          <div>
            <h3
              class="text-sm font-bold text-slate-400 uppercase tracking-widest mb-6 border-b border-slate-100 pb-2"
            >
              2. Votre Besoin Critique
            </h3>

            <div class="space-y-6">
              <div>
                <label for="contexte" class="block text-sm font-bold text-slate-700 mb-2"
                  >Quel est le processus à sécuriser ?</label
                >
                <textarea
                  name="contexte"
                  id="contexte"
                  rows="3"
                  required
                  class="w-full px-4 py-3 rounded-xl bg-slate-50 border border-slate-200 focus:border-rust focus:ring-1 focus:ring-rust outline-none transition-colors"
                  placeholder="Ex: Validation manuelle de factures fournisseurs, Surveillance de vibrations moteur..."
                ></textarea>
              </div>

              <div>
                <label class="block text-sm font-bold text-slate-700 mb-2"
                  >Vos contraintes majeures</label
                >
                <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                  <label
                    class="flex items-center gap-3 p-4 border border-slate-200 rounded-xl cursor-pointer hover:bg-slate-50 transition-colors"
                  >
                    <input
                      type="checkbox"
                      name="contraintes"
                      value="Sécurité"
                      class="text-rust focus:ring-rust rounded"
                    />
                    <span class="text-sm font-bold text-slate-700">Sécurité</span>
                  </label>
                  <label
                    class="flex items-center gap-3 p-4 border border-slate-200 rounded-xl cursor-pointer hover:bg-slate-50 transition-colors"
                  >
                    <input
                      type="checkbox"
                      name="contraintes"
                      value="Souveraineté"
                      class="text-rust focus:ring-rust rounded"
                    />
                    <span class="text-sm font-bold text-slate-700">Souveraineté</span>
                  </label>
                  <label
                    class="flex items-center gap-3 p-4 border border-slate-200 rounded-xl cursor-pointer hover:bg-slate-50 transition-colors"
                  >
                    <input
                      type="checkbox"
                      name="contraintes"
                      value="Conformité"
                      class="text-rust focus:ring-rust rounded"
                    />
                    <span class="text-sm font-bold text-slate-700">Conformité</span>
                  </label>

                  <label
                    class="flex items-center gap-3 p-4 border border-slate-200 rounded-xl cursor-pointer hover:bg-slate-50 transition-colors"
                  >
                    <input
                      type="checkbox"
                      name="contraintes"
                      value="Volume"
                      class="text-rust focus:ring-rust rounded"
                    />
                    <span class="text-sm font-bold text-slate-700">Volume de Données</span>
                  </label>
                  <label
                    class="flex items-center gap-3 p-4 border border-slate-200 rounded-xl cursor-pointer hover:bg-slate-50 transition-colors"
                  >
                    <input
                      type="checkbox"
                      name="contraintes"
                      value="Latence"
                      class="text-rust focus:ring-rust rounded"
                    />
                    <span class="text-sm font-bold text-slate-700">Latence (Temps Réel)</span>
                  </label>
                  <label
                    class="flex items-center gap-3 p-4 border border-slate-200 rounded-xl cursor-pointer hover:bg-slate-50 transition-colors"
                  >
                    <input
                      type="checkbox"
                      name="contraintes"
                      value="Time-to-Market"
                      class="text-rust focus:ring-rust rounded"
                    />
                    <span class="text-sm font-bold text-slate-700">Time To Market</span>
                  </label>
                </div>
              </div>

              <div>
                <label for="message" class="block text-sm font-bold text-slate-700 mb-2"
                  >Message libre</label
                >
                <textarea
                  name="message"
                  id="message"
                  rows="4"
                  class="w-full px-4 py-3 rounded-xl bg-slate-50 border border-slate-200 focus:border-rust focus:ring-1 focus:ring-rust outline-none transition-colors"
                  placeholder="Détaillez ici vos attentes..."></textarea>
              </div>
            </div>
          </div>

          <div class="text-center pt-4">
            <button
              type="submit"
              id="submit-btn"
              class="bg-slate-900 hover:bg-slate-800 text-white font-bold py-4 px-12 rounded-full transition-all shadow-lg hover:shadow-xl hover:scale-105 transform duration-200 flex items-center justify-center gap-2 mx-auto disabled:opacity-50 disabled:cursor-not-allowed"
            >
              <span>Envoyer la demande</span>
              <span class="text-rust">→</span>
            </button>
          </div>
        </form>
      </div>
    </main>

    <Footer />

    <script define:vars={{ GOOGLE_SCRIPT_URL }}>
      const form = document.getElementById('audit-form');
      const submitBtn = document.getElementById('submit-btn');
      const successMessage = document.getElementById('success-message');

      form.addEventListener('submit', (e) => {
        e.preventDefault();

        // Etat chargement
        submitBtn.disabled = true;
        submitBtn.innerHTML = 'Transmission en cours...';

        // Récupération des données
        const formData = new FormData(form);

        // Gestion spéciale pour les checkbox multiples (contraintes)
        // On les rassemble dans une seule chaîne de caractères
        const contraintes = formData.getAll('contraintes').join(', ');
        formData.set('contraintes', contraintes);

        // Envoi vers Google Script
        fetch(GOOGLE_SCRIPT_URL, { method: 'POST', body: formData })
          .then((response) => {
            // Succès : On affiche le message
            successMessage.classList.remove('hidden');
            successMessage.classList.add('flex');
            form.classList.add('opacity-0'); // On masque le formulaire subtilement
          })
          .catch((error) => {
            console.error('Erreur!', error.message);
            submitBtn.innerHTML = 'Erreur - Réessayez';
            submitBtn.disabled = false;
          });
      });
    </script>
  </body>
</html>
